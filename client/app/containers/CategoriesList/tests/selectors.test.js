import {
  selectCategoriesList,
} from '../selectors';
import { fromJS } from 'immutable';
import expect from 'expect';

const selector = selectCategoriesList();

describe('selectCategoriesList', () => {

});
