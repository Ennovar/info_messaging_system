import {
  selectCustomNavbar,
} from '../selectors';
import { fromJS } from 'immutable';
import expect from 'expect';

const selector = selectCustomNavbar();

describe('selectCustomNavbar', () => {

});
